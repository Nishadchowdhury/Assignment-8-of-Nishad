import React from 'react';

const SingleCartI = (props) => {
    const {name} = props.data;

    return (
        
        <div>
            {name}
        </div>
    );
};


export default SingleCartI ;